# QoderWake 实地观察（2026-09-10 P0 返工轮）

> 实例 http://127.0.0.1:19830（QoderWake CN），IAB 浏览器实地操作 + DOM snapshot + 几何测量。
> 视口：默认 1548×871（测量值注明）；展开/收起态分别测量。
> 纪律：只读观察；创建/运行/启停等副作用控件不点（新建自动任务弹窗打开后取消；角色详情/自定义 Waker 弹窗只看不提交）。

## 1. 全局导航（左轨）

收起态（窄轨 64px）：
- complementary "Chat 对话" x=0 w=64 全高；品牌 mark 顶部；"展开侧边栏" 按钮。
- nav 项：h=32 w=64 radius=4px 图标 16px；y 步长 36（间距 4）；分组 heading 收起时隐藏。
- 底部：用量/设置 图标按钮 28×28（y≈791/831）+ 用户头像块（y≈728 h=62）。

展开态（240px）：
- 品牌行 h=48（strong "QoderWake CN"）；折叠按钮 20×20 @ (208,14)。
- 分组 heading：fs=10px fw=400 color rgb(131,130,128) h=20（"工作管理" y=48 / "员工资源" y=192）。
- nav 链接：h=32 w=216 fs=13px fw=400 radius=4px padding 4px 8px color rgb(99,98,97)；y 步长 36。
- tablist 员工与群组（Waker(n)/Group(n)）y=300 h=34 —— **我方不引入 Group（任务书）**。
- "新建 Waker" 按钮 + 搜索框（placeholder 搜索 Waker）。
- 员工卡：h=56（avatar+名称+描述两行），间距 4。
- 底部账号区：头像+用户名 strong+团队版+用量/设置图标按钮。
- 设置入口 = 底部齿轮图标按钮 → 弹出菜单（.qc-settings-menu-item）：设置 / 主题▸ / 语言▸ / 退出登录 + 用户块。**菜单内"设置"项进 /settings/preferences；全局唯一设置入口**。

## 2. 对话页 /conversations/qs_<id>?sid=<uuid>（P0-D 参照，截图 qw-ref-conversation-1548.png）

三栏（实测 1548 视口）：
- 左：全局窄轨 64 + **对话历史侧栏 x=64 w=240 全高**（complementary "<Waker名>"）：
  - Waker 身份链接（avatar+名称 → /wakers/<id>/home）+ "管理" 按钮；
  - tablist "对话历史"：对话任务 [selected] | 自动任务（segmented pill）；
  - "3 个任务" + "+ 新建" 按钮；
  - 任务条目按钮：chip（如"暂无待关注结果"，仅真实 HITL 态存在时）+ 任务名 + 日期 + "更多操作"⋯；选中项高亮底；
  - separator "调整对话任务栏宽度"（可拖宽）。
- 中：任务标题头（左任务名，右 "当前任务" 按钮 96×28）+ 消息流（居中列 ~720 宽）+ composer。
  - 用户消息：qc-user-message-shell（右偏、max-w 内）+ time + 复制按钮。
  - 助手消息：avatar + 名称 + "深度思考" 折叠按钮（ThinkingBlock 事件才渲染）+ 正文段落/列表/代码块 + 工具卡。
  - 工具卡：button "已完成 <工具名> <摘要>"（状态词+名称+单行摘要），展开显示 参数/响应 两块；多步聚合为 "查看 N 个步骤" 折叠组。
  - **产物 = 助手消息内嵌 inline section**：header "产物(7)" + 文件行（名称+描述+文件名 chip）+ "剩余N个" 折叠。
  - 消息时间戳行（2026-09-08 15:21:42）。
  - composer：textarea "输入消息，@ 选择当前工作区上下文..." + 底行（选择工作目录 按钮 + "+" + 模型选择 "Qwen3.7-Max" + 圆形发送按钮[空时 disabled]）。
- 右：**产物/当前任务面板 x=1309 w=240（y=44 起，h=827）**，由头部 "当前任务" 按钮 toggle：
  - 标题 "产物"；"当前工作目录"（外链图标）；"工作区文件" 区：当前工作区文件变更（N 个文件变更 + 文件行 + 剩余N个）；"最终文件" 区（文件行：名称+描述 + 剩余N个）。
- 1548 视口下右面板默认收起（需点 "当前任务" 打开）；无右面板时消息列居中于剩余空间。

**裁决记录**：上一轮调研（12 号 §3）记 "历史面板在右栏" 与本轮实测不符——本轮实测历史侧栏在**左**（x=64 w=240），与任务书 §六 一致；产物为**内嵌卡 + 可 toggle 右面板**双形态，任务书 "右：当前任务产物" 由右面板满足。

## 3. 新建 Waker /recruitment-market（P0-E 参照）

- 面包屑 nav "招聘市场导航"：Waker 管理（返回按钮）/ 创建 Waker。
- h1 创建 Waker + 副文案 "选择适合工作场景的预置 Waker，直接使用或按需调整；若没有合适的角色，可以创建自定义 Waker。" + 右上 "自定义 Waker" 按钮。
- 角色模板卡 list（article×12）：10 预置角色（前端/后端/测试工程师、产品经理、数据分析师、UI 设计师、内容运营专员、DevOps 工程师、项目管理员、运维工程师）+ 自定义模板卡（带 ⋯更多操作）+ 群聊答疑专员。
  - 卡内容：名称 + 描述（2-3 行截断）+ 3 个能力 tag chip。
- 详情 = 右侧抽屉 dialog（x=589 w=960 全高）："员工详情" + 名称 + 描述 + 按钮 [返回列表] [就他了！(基于模板创建)]；
  分区：核心能力（5）（emoji+名称·副题+描述）/ 工作风格（chips）/ 工作模式（3 模式各带流程串）/ 技能（8）（skill 名 chips）。
- 自定义 Waker = 居中 modal 800×729："自定义 Waker 角色" + 副文案 "这里创建的是角色模板。保存后，你可以基于该模板创建多个 Waker。"
  - 角色名称* / 它主要负责什么?* / 角色配置 tabs（智能生成|上传 Markdown|手动填写）+ 生成配置 / 技能(0)（插件市场选择|上传）/ 连接器(0)（添加连接器）/ [创建角色][取消]。

## 4. 设置 /settings/preferences（P0-F 参照）

- 入口唯一：左轨底部齿轮 → 菜单 → "设置"。
- **二级侧栏 complementary "通用"：x=240(展开态)/64(收起态) y=0 w=240 全高**；内容：返回按钮 + nav（偏好设置/系统设置/网络诊断/管理环境与设备/更新应用）。
- main x=480（展开态）：h1 偏好设置 + 副文案 + 设置行（标题+说明+控件）：界面语言/AI 回复语言/主题亮暗。
- /settings/system：开机自动启动/保持系统唤醒/允许远端配置管理（switch；实验室特性 chip）。
- 展开态实测：rail 240 + 二级 240 + main 480 起 —— 与任务书 §八 完全一致。

## 5. 自主工作 /autonomous-work（P0-G 参照）

- h1 自主工作 + 副文案 "通过定时、事件或 API 自动开工，并由 Waker 或 WakerFlow 响应。" + 右上 "新建自动任务"。
- 指标带 4 卡：自动任务总数/已启用/Waker 执行/WakerFlow 执行（strong 数字+label+说明三行）。
- 筛选 4 combobox：执行者（全部执行者）/触发类型（全部类型）/自动任务状态（全部状态）/排序（最近创建）。
- 表 7 列：自动任务/触发来源（API chip）/触发条件（"通过 POST 请求触发"）/执行者（avatar+strong 名+类型 Waker|WakerFlow）/最近触发（—）/累计自动运行（N 次）/状态（switch 启用[checked]+label）。
- 分页：共 N 条 + 上一页/1/下一页 + 每页条数 combobox（10 条/页）。
- 新建弹窗 800×807：名称* / 触发条件*（触发方式卡 定时|API；调度类型 定期|一次性；重复+下次运行；添加触发方式；1/5）/ 执行方式*（交给 Waker|运行 WakerFlow 卡）/ **执行对象*（真实选择器：显示选中对象名+描述+"更换"按钮）** / 执行指令*（Prompt + Auto + 0/10000）/ 工作空间（默认工作空间|本地目录|项目）/ 高级设置（折叠）/ [取消][保存]。

## 6. Waker 管理 /management 与 Waker 工作区 /wakers/<id>/home

- /management：h1+副文案+动作（分享记录/导入 Waker/新建 Waker）+ tablist 管理分类（Waker|Group）+ 筛选（搜索名称或角色/运行状态默认在线/角色/环境/排序字段）+ 计数 "3 个 Waker" + 卡（在线徽标/本机+主机名/avatar+h2 名/角色 chip/描述/任务数/最近运行/管理·分享·对话按钮/"查看 X 的角色详情" 链接）。
- /wakers/<id>/home：**二级侧栏 "Waker 详情导航" x=240 w=240 全高**（返回 + 分组 nav：概览 / 工作(任务看板,自主工作) / 记忆与学习(记忆,自进化 Skill) / 能力与资源(Skill,连接器,Wakerflow,知识库,项目) / 权限与管理(权限,Waker 档案)）；main x=480。
- 概览页：头部（首页+分享+删除 Waker）+ 身份区（avatar+ID+h1 名+角色 chip+编辑+在线+入职时间+描述）+ 工作日志统计 + 活跃度热力图（月×周几网格，cell aria "YYYY-MM-DD, 当日任务数：N"）。
- 实测 documentElement scrollW==clientW、scrollH==clientH（1548×871 无溢出）。

## 7. 任务看板 /work-management（复核）

与 09-07 对账一致：指标带（任务总数/进行中/需要操作/已结束 + 数据周期）+ "需要关注" region（需要操作/查收结果 tab + 空态文案）+ 全部任务（列表|泳道 tab + 搜索 + Waker/Group + 触发方式 + 任务状态 + 数据周期 + 表 5 列 任务/执行者/来源/状态/最近更新 + 行内查看详情 + 分页 + 每页条数）。
