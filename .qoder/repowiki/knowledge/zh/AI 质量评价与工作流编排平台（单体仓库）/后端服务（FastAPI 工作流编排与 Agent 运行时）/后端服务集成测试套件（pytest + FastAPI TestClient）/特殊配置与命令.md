运行单个测试需确保模块顶部已调用 `start_worker()`，否则 Agent/Runner 的异步执行无法到达终态；`test_contracts.py` 中的 quickservice 黄金 fixture 路径位于 `docs/research/lightweight-workflow-system/evidence/network/flow-detail-decoded-dsl.json`，缺失时该用例会被 `@pytest.mark.skipif` 跳过。