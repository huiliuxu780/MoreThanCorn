运行前需启动后端 `uvicorn app.main:app --port 8100` 并用 `--remote-debugging-port=9222` 启动 Chrome 指向 `localhost:5173`；`verify-fullstack.mjs` 可通过 `VERIFY_BASE` 环境变量切换后端地址；所有 `.mjs` 脚本均以 `process.exit(0)` 结束并通过截图落盘 `/tmp/q*` 供人工复核。