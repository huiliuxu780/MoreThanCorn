- 页面组件一律通过 `React.lazy(() => import("@/pages/..."))` 动态导入，并由顶层 `Suspense` 提供骨架回退。
- 路由按业务域分组注释（智能质检 / 配置管理 / 系统级设置），并使用 `Navigate` 做旧路径收敛重定向。
- 全局 UI 能力（如 TooltipProvider）在 `main.tsx` 最外层 Provider 中一次性注入，而非在各页面重复包裹。
- 主题色全部通过 CSS 自定义属性（oklch 变量）+ `@theme inline` 暴露，禁止在组件中硬编码 hex 色值。