TypeScript + Vite 环境变量（`VITE_WF_API`/`VITE_WF_API_BASE`/`VITE_WF_API_TOKEN`）控制是否启用真实后端；HTTP 客户端基于原生 fetch，认证通过 `Authorization: Bearer <token>` 头传递；SSE 事件流通过 `fetch` + `ReadableStream` 解析；RBAC 使用纯函数式权限矩阵，无状态存储。