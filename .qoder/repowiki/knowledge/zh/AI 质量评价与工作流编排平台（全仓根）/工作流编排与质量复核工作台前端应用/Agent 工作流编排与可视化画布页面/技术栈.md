React + TypeScript + React Router；可视化画布使用 `@xyflow/react`（React Flow）；UI 基于 shadcn/ui（Dialog、Popover、Button、Input、Select、Switch 等）；通知使用 `sonner` toast；图标来自 `lucide-react`；代码编辑使用 `@uiw/react-codemirror` 配合 Python 语言扩展。