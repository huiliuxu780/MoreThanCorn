- 脚本以顶层注释标注对应 SDD 章节编号（如 §9.1/V1/V4、§9.2/§9.3/V2/V3），将实现与规范文档绑定。
- 通过命令行参数（`--seed`、`--update-baseline`）切换脚本行为，而非环境变量或配置文件。
- 对敏感变更采用“基线+增量”策略：允许列表与视觉基线只增不减，新增命中需显式批准。
- 通过相对路径 `new URL("..", import.meta.url).pathname` 解析仓库根目录，避免硬编码绝对路径。