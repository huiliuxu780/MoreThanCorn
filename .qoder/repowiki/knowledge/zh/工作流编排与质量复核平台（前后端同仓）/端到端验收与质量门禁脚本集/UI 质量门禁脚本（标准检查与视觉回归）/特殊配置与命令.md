`node scripts/check-ui-standard.mjs --seed` 可重新生成 `ui-allowlist.json` 基线；`node scripts/check-visual-regression.mjs --update-baseline` 用于在 UI 变更后刷新 `scripts/visual-baseline/` 下的 PNG 基线。运行视觉回归前需保证 dev server（端口 5173）与后端服务（端口 8100）已启动，且系统存在 Google Chrome 可执行路径。