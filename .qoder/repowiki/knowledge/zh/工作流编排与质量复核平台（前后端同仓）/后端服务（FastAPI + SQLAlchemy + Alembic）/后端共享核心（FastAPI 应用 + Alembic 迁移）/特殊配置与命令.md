环境变量：`WF_DATABASE_URL`（PostgreSQL DSN，pytest 下自动切换至 wf_test）、`WF_API_TOKEN`（启用 /api/* Bearer 鉴权）、`WF_LLM_BASE_URL`/`WF_LLM_API_KEY`（真实 LLM 接入）、`WF_SECRET_KEY`（Fernet 密钥，缺失时明文回退）。迁移命令：`alembic revision --autogenerate -m "..."` 生成脚本后 `alembic upgrade head` 或 `alembic downgrade <rev>`。