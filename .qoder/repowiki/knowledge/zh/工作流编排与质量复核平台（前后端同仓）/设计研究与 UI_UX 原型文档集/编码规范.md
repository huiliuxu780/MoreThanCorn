- 设计文档采用递增数字前缀命名（如 01-、02-、…、17-）并按序号顺序编排，体现从调研到实现的渐进式演进路径。
- 证据类内容统一放入 `evidence/` 子目录，并按 network、screenshots 等来源类型分文件夹归档，同时维护 `evidence-ledger.md` 台账索引。
- 对外契约（API、事件、DSL）集中置于 `contracts/` 目录并以 OpenAPI YAML 与 JSON Schema 形式声明，作为前后端唯一事实源。
- 原型页面以独立 HTML 文件拆分不同视图（index、ai-resources、data-resources、wizard、detail-*、components），并通过共享的 `assets/app.js` 与 `assets/style.css` 复用样式与逻辑。