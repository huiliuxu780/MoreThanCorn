启用真实后端需设置 `VITE_WF_API=1` 并在 `localStorage` 写入 `wf_api_token`（或通过 `VITE_WF_API_TOKEN` 注入），否则所有请求指向 `http://127.0.0.1:8100`；切换 RBAC 角色需在 `localStorage` 写入 `wf_role`（viewer/editor/publisher/admin），或通过 `rbac.login` 登录获取服务端角色。