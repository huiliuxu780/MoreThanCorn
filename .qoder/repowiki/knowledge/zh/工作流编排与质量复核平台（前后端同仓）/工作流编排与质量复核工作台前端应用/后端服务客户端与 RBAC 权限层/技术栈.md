TypeScript + Vite 环境变量（`VITE_WF_API`/`VITE_WF_API_BASE`/`VITE_WF_API_TOKEN`）控制真实后端开关；HTTP 客户端基于原生 fetch，认证通过 `Authorization: Bearer <token>` 头传递；SSE 事件流通过 `ReadableStream` + `TextDecoder` 解析；RBAC 使用纯函数式权限矩阵，无外部状态库。