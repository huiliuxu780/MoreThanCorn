# 2026-09-11 全量审计（代码/产品/UI）合并报告与处置

方法：双只读审计代理并行（产品需求缺口 32 条 + 代码/UI 36 条），逐条给 file:line 证据；本轮已修项标注 ✅，遗留项标注 ⏳ 并给建议批次。

## 一、已修（本轮落地，门禁背书：pytest 493 / vitest 65 / ui-standard / theme4 36 / typecheck 0）

### 安全（P0/P1）
- ✅ `PATCH /api/v2/agentflows/{fid}` 补 `require_role` 鉴权（as_flows_board.py）+ 回归测试在 test_agentflow_governance_0911。
- ✅ `GET /api/agents/{aid}/releases` 补 `require_role`（agents.py）。
- ✅ release `actor` 改取自已认证主体，请求体伪造无效 + 防伪回归测试。

### 产品 P0
- ✅ **AgentFlow 编辑器死胡同**：详情页补最小节点编辑器——「添加阶段」（id/Agent 选择/Prompt）与「删除阶段」（确认对话框），保存即新版本；空态给「添加首个阶段」CTA。纯 UI 用户现在可以造首版本→发布→运行。

### 用户报的三 bug
- ✅ 执行过程对历史会话恒 0 → 从持久化消息推导（模型调用/工具/思考计数）+ AgentActivity 历史态。
- ✅ 思考区下滚空白 → 消息列改 bottom-anchor（min-h-full + justify-end），短会话不再顶部堆、底部大空白。
- ✅ 「无法新建对话」→ 自动选中最近会话 effect 吞掉新建意图；加显式 draftNew 草稿态（新建=空 composer+「新对话」标题，首条发送建会话，选旧会话退出草稿）。
- ✅ 「自动滚动是什么」→ 开关移除，改读者感知：贴底自动跟随、上读即停、「回到最新」浮标。

### i18n / a11y / 运行时小患（P1/P2 批修）
- ✅ ApprovalCard：rejectLabel prop+「拒绝」、状态徽章全中文、latent 串中文化；HITL 拒绝路径补测试。
- ✅ StreamingResponse「N 个来源」、PromptInput aria/placeholder/选择模型、AgentProgress aria、message「回复中」、bubble 展开/收起默认。
- ✅ agent-chat 右栏状态/触发裸枚举套中文映射；toolBadge 技能/知识/工具；node-run-panel 状态/工具/attempt 中文 + REPLY_END 回读收尾。
- ✅ run-detail 交互执行区块中文化（含枚举 label）。
- ✅ agentflow-detail：attempt 中文、铅笔 aria 改「调整步骤说明」、hex 兜底移除、节点卡 Space 键、添加/删除阶段。
- ✅ wf-agents-list 段控补 radiogroup/radio/aria-checked。
- ✅ agent-chat 4s 对账定时器纳入 abort 生命周期；未识别事件台账组合 key。
- ✅ 死件删除：chat/deep-thinking、beui tool-approval/reasoning-text/todo-list/code-block/message-scroller(+re-export)/motion loader/text-scramble/preview-rail/button metallic。

## 二、遗留（⏳ 建议批次，均不阻断当前验收）

### 产品链缺口（产品审计 P1/P2）
- ⏳ B1：AgentFlow 运行观测断链——运行时 flow SSE 未代理、节点事后落库（agentflow_executor 同步阻塞）；建议入 JobQueue 异步 + 增量落 node_run + 平台代理 flow SSE。
- ⏳ B1：批次观测增强（06 文档 P1–P7）整份未动：进度列/错误聚合/取消端点/batch 粒度告警/SSE 增量；与攒批方案（13 号稿 S1–S4）合批实施。
- ⏳ B2：MQ consumer（doc03 §4.2）、OTel/Tracing（doc11 R2）、OSS 真实探测、MySQL reader（或向导明示不可读）、Knowledge embedding 凭据+真冒烟、KB 删除/文档管理、MCP tools/list 发现。
- ⏳ B2：审计日志页重做（服务端分页/筛选/错误态）；质检两 dashboard 按拍板改重定向/提示页；tasks 列表服务端分页。
- ⏳ B3：对话附件上传、@ 上下文 mention（或改 placeholder 不承诺）；管理触发方式按钮改跳转+预过滤；composer 工作目录改只读展示；Run 增 worker 标识；doc12 工单 Outbox 写回；并发/misfire 治理；trigger=api 死字段清理；release() 死参数清理；看板 @Waker 永假筛选项摘除；侧栏个人资料立项或移除；记忆 timeline skill 事件源改接 Release 冻结变更；admin.py 重复 POST /api/models 删一。

### 代码/UI 遗留
- ⏳ trace-view 全页硬编码浅色（dark 必破）+ 旧页面零散 hex（module-agent-config/agent-common-config/form-renderer/wf-connections/AgentWorkspaceShell）——需设计过一遍的 token 化专批，未擅动防视觉回归。
- ⏳ thinking 白名单静默丢弃无日志、值无范围校验（发布期前置校验建议）。
- ⏳ node-run-panel 断线重连（REPLY_END 已收尾，onDone 重连未接）。
- ⏳ popover-morph 阴影 rgba（黑阴影主题中性，登记例外）。
- ⏳ 测试补：读者感知滚动（jsdom 无布局，需 stub scrollHeight 或 e2e）、node-run-panel 清理断言。
- ⏳ visual-regression gate 硬编码退役栈 5173/8100 → 重指向新栈属独立工单；逐屏签字待用户。

## 三、09-11 权限策略层（P1 已落地并活体闭环）
- 页面：工作台「安全与权限」子页（四概览卡 tab+主开关联动灰化+三态表+六类规则+逃逸固定层+诚实占位），lucide 图标；
- 契约：config.permissions v2 → release 冻结 frozen_permission_policy（mode/deny_tools/ask_tools/bash patterns/sensitive globs）→ manifest 下发 → 运行时 permission_context 注入；
- 活体：master 开→v6 冻结 accept_edits+12ask/4deny→sudo 回合 awaiting_permission→对话页 ApprovalCard 截图→未确认不执行（会话 idle 收尾）；
- 测试：test_agentflow_governance_0911 5/5（含 v2 冻结规格断言）；门禁 typecheck0/vitest65/ui-standard 绿。
- 坑登记：agentApi.update config 整体替换须合并写；8120/8301 改码后须带 MTC_INTERNAL_TOKEN 重启。

## 三b、09-11 权限策略层旧稿（用户问「agent 基础权限能否 UI 控制」后落地）
- 六开关组（shell/file_write/file_read/schedule/subagent/platform_tools），默认全开 opt-out；config.permissions → 发布冻结 frozen_tool_policy → session-manifest 下发 → 运行时包装官方 get_toolkit 后 remove_tool 摘除（_chat/_service from-import 绑定需同步替换模块引用；manifest 双头校验需带会话令牌）。
- 活体验证：smoke agent 关 shell 重发布后，模型自述「没有 bash 执行工具」改用 Glob（file_read 仍开）；配置页六开关 UI + 治理页「权限收紧」chip；冻结测试 4/4。

## 四、09-11 夜事故登记
- 手动重启 8120 漏 MTC_INTERNAL_TOKEN → internal 端点 501 → 运行时工具清单拉取失败 → smoke 会话 02:19 回合「unknown error」。已带 token 重启恢复（无 token 401=传输门正常）；操作规约入记忆 morethancorn-dev-environment。顺带修工具结果 JSON 双重转义显示（unwrapJsonString）。

## 四、三执行链+automations+batch 四环节结论（产品审计原表精简）
- Agent 对话：四环节齐（缺附件/@ 上下文）。
- Workflow：齐（决策/路由节点模型硬编码 qwen-plus 待节点 schema modelRef）。
- AgentFlow：入口已修（本轮编辑器）；执行同步阻塞/无取消、观测断链待 B1。
- Automations：四通道齐（MQ=UNC）；并发/misfire/攒批待 B1/B2。
- Batch：执行/投递闭环；观测增强+取消待 B1（攒批窗口设计见 13 号稿）。
