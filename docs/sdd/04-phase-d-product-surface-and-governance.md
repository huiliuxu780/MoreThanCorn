# Phase D｜产品面与治理（细化版）

状态：**规格已细化，未开工**（按夜间施工边界：A/B/C 实现，D 只细化规格）
定位：持续投入阶段，建议按 2 周一个迭代切片，逐切片冻结后开工
对标：调研 02/03（壳层交互）、07 §6–8（观测/评测/进化）、13（安全治理）、00 §19（交付物）

---

## 1. 切片划分（每个切片独立冻结/验收）

### D-1 Agent Workspace Shell 完整化（约 1 周）
- Header 四 Tab（搭建/运行观测/效果评测/进化）三型统一入口（当前观测/评测只在对话编排画布抽屉）。
- 运行观测页：Agent 维度运行列表 + 指标聚合（首 token、平均时长、错误率、token 消耗——数据源 `run_event.tokens`/`duration_ms` 已就位）。
- Trace 视图：按 span 树展示（`span_id/parent_span_id` 已就位）；不展示隐藏推理全文（调研 00 §9.6）。

### D-2 预览增强（约 1 周）
- 模型对比：双卡共输入框，会话级配置不进版本（调研 07 §2 结论 + 02 §3 实测交互）。
- 预览会话持久化（会话历史、清空会话）。
- 消息反馈（赞/踩仅存储）。

### D-3 评测与进化真语义（约 2 周）
- 评测：数据集版本化 + 字段映射（query/chatHistory）+ Judge 三类（规则/模型/人评）+ 样本级分数报告。
- 进化：失败归因 → CandidatePatch（可查看/可撤销）→ 回归评测 → 人工审批 → 新草稿；进化永不直接改线上版本（调研 07 §8）。
- "版本指标"面板恢复"进化"名实。

### D-4 治理（约 2 周）
- RBAC 四角色（Viewer/Editor/Publisher/Admin）替换 `rbac.ts` 恒真桩；资源权限分"可见/可绑定/可试运行/可发布"。
- 审计日志表：发布/回滚/删除/解锁全留痕（真实操作人替换"质量管理员"硬编码）。
- 编辑锁升级租约（持有者/过期/续租/请求接管）。
- Secret 治理：轮换流程 + Trace 脱敏策略。

### D-5 交付物与清退（约 1 周）
- 调研 00 §19 十项：`agent-domain.openapi.yaml`、definition/graph/node-registry/run-event 四份 JSON Schema、错误码表、E2E 验收用例集等。
- 前端测试框架（vitest）+ 关键纯函数覆盖（祖先可达、绑定解析、事件归并）。
- 质检页面 mock 清退（tasks/result-rules/agent-analysis 对 `mock-service` 的依赖）→ 最终删除 `mock-service.ts`/`mocks/`。

## 2. 承接 Phase C 的推迟项（候选切片，逐项评估后排期）
| 项 | 推迟原因 | 前置 |
| --- | --- | --- |
| 信息收集节点（提问-暂停-恢复） | 需要运行检查点/恢复机制 | 运行挂起状态机 |
| 数据查询节点（NL2SQL） | 依赖数据目录与连接治理 | D-4 凭据治理 |
| 图像生成节点 | 依赖生图模型接入 | 模型网关注册 |
| ValueBinding 全结构化迁移 | 展示层重构，校验已覆盖可达性 | vitest 保障（D-5） |
| 终端用户对话渠道（发布后真实用户会话） | 未建模；需要会话隔离与渠道接入 | Release 语义扩展 |
| 容量加固（worker 池/SSE 推送/分区归档） | 触发门槛：日 run ≥10 万或峰值 ≥50/s（决策 D-5） | 压测基线 |

## 3. 验收基线（映射调研 §15.5 / 00 §19 / 07 §7–8）
- [ ] 发布前后端双重校验；沙箱与线上独立部署记录；回滚进入审计日志（§15.5）
- [ ] 评测任务可复现到 Agent 版本 + 数据集版本；Judge 打分样本级可查（07 §7）
- [ ] 进化只产出候选 Patch，审批后生成新草稿；不触碰线上版本（07 §8）
- [ ] 四角色权限矩阵生效；高危操作全部有审计（调研 13）
- [ ] 十项工程交付物齐备且与代码一致性抽查通过（00 §19）
- [ ] `grep -rn "mock-service" src` 归零（D-5 完成标志）

## 4. 状态日志
- 2026-08-25 大纲建立。
- 2026-08-25 夜间细化为切片规格（用户授权连续施工的边界：D 不实现）；开工前逐切片冻结。
