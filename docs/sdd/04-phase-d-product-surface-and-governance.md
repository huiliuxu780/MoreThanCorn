# Phase D｜产品面与治理（细化版）

状态：**规格已细化，未开工**（按夜间施工边界：A/B/C 实现，D 只细化规格）
定位：持续投入阶段，建议按 2 周一个迭代切片，逐切片冻结后开工
对标：调研 02/03（壳层交互）、07 §6–8（观测/评测/进化）、13（安全治理）、00 §19（交付物）

---

## 0. D-1 切片冻结（2026-08-26，用户指令"直接开始 Phase D"）

**范围（全部真功能，不留假门）**：
1. 三型统一 Tab：autonomous/expert-group 页增加 运行观测 / 效果评测 / 版本指标 入口（对话编排已在画布）。
2. Agent 级运行观测：指标卡（总数/成功率/平均时长）+ 运行列表 + 事件时间线。
3. Agent 级评测：样本挂 `agent_id`，eval-run 走 `run_agent`（同步），汇总成功率（Judge 仍属 D-3）。
4. 专家组画布化：expert-group 进设计器（agentMeta GROUP → 目录收敛 7 类），左侧成员池入抽屉；Agent选择节点的主要/兜底成员从成员池选。
5. 自主规划：Prompt 模板库 + AI 生成（真 LLM）；知识高级配置（TopK/匹配分/检索模式，真消费）；开场白（预览首条消息）。
6. 成员冻结版本：版本列表返回 AGENT 依赖冻结摘要。

**不做（留 D-2/D-3）**：模型对比、模型语义参数、分句/语音、`#` mention 插入（需 Prompt 变量解析）、进化真语义。

**验收**：①三型四 Tab 可达；②观测指标与运行明细一致；③评测样本→运行→成功率闭环；④专家组画布目录 7 类+成员选择联动；⑤模板/生成/高级配置真生效；⑥81+ 测试绿。

**完成记录（2026-08-26）**：全部落地，提交 `c657bab`（migration 018/019）。85/85 pytest 绿（`test_phase_d1.py` 4 条新增）；无头真机验证（`scripts/check-d1.mjs`）：自主规划四 Tab ✓、模板库+AI 生成 ✓、观测面板 ✓、专家组画布化 ✓、无页面报错。**待用户逐项验收。** 已知：`test_runner::test_event_sequence_monotonic_and_terminal` 偶发时序不稳（共享测试库 worker 竞争），单独/重跑均绿，未深追。

## 1. 切片划分（每个切片独立冻结/验收）

### D-1 Agent Workspace Shell 完整化（约 1 周）
- Header 四 Tab（搭建/运行观测/效果评测/进化）三型统一入口（当前观测/评测只在对话编排画布抽屉）。
- 运行观测页：Agent 维度运行列表 + 指标聚合（首 token、平均时长、错误率、token 消耗——数据源 `run_event.tokens`/`duration_ms` 已就位）。
- Trace 视图：按 span 树展示（`span_id/parent_span_id` 已就位）；不展示隐藏推理全文（调研 00 §9.6）。

### D-2 预览增强（约 1 周）
- 模型对比：双卡共输入框，会话级配置不进版本（调研 07 §2 结论 + 02 §3 实测交互）。
- 预览会话持久化（会话历史、清空会话）。
- 消息反馈（赞/踩仅存储）。

### D-3 评测与进化真语义（约 2 周）
- 评测：数据集版本化 + 字段映射（query/chatHistory）+ Judge 三类（规则/模型/人评）+ 样本级分数报告。
- 进化：失败归因 → CandidatePatch（可查看/可撤销）→ 回归评测 → 人工审批 → 新草稿；进化永不直接改线上版本（调研 07 §8）。
- "版本指标"面板恢复"进化"名实。

### D-4 治理（约 2 周）
- RBAC 四角色（Viewer/Editor/Publisher/Admin）替换 `rbac.ts` 恒真桩；资源权限分"可见/可绑定/可试运行/可发布"。
- 审计日志表：发布/回滚/删除/解锁全留痕（真实操作人替换"质量管理员"硬编码）。
- 编辑锁升级租约（持有者/过期/续租/请求接管）。
- Secret 治理：轮换流程 + Trace 脱敏策略。

### D-5 交付物与清退（约 1 周）
- 调研 00 §19 十项：`agent-domain.openapi.yaml`、definition/graph/node-registry/run-event 四份 JSON Schema、错误码表、E2E 验收用例集等。
- 前端测试框架（vitest）+ 关键纯函数覆盖（祖先可达、绑定解析、事件归并）。
- 质检页面 mock 清退（tasks/result-rules/agent-analysis 对 `mock-service` 的依赖）→ 最终删除 `mock-service.ts`/`mocks/`。

## 2. 承接 Phase C 的推迟项（候选切片，逐项评估后排期）
| 项 | 推迟原因 | 前置 |
| --- | --- | --- |
| 信息收集节点（提问-暂停-恢复） | 需要运行检查点/恢复机制 | 运行挂起状态机 |
| 数据查询节点（NL2SQL） | 依赖数据目录与连接治理 | D-4 凭据治理 |
| 图像生成节点 | 依赖生图模型接入 | 模型网关注册 |
| ValueBinding 全结构化迁移 | 展示层重构，校验已覆盖可达性 | vitest 保障（D-5） |
| 终端用户对话渠道（发布后真实用户会话） | 未建模；需要会话隔离与渠道接入 | Release 语义扩展 |
| 容量加固（worker 池/SSE 推送/分区归档） | 触发门槛：日 run ≥10 万或峰值 ≥50/s（决策 D-5） | 压测基线 |

## 2.1 编辑页补全清单（08-26 用户追问后补登记；对标调研 02/03/06/11）
**自主规划页已完成**（A/B/C）：角色 Prompt、模型选择、技能/插件/工作流/知识挂载（真选择器）、结构化记忆表单、对话体验（续问+闲聊兜底）、流式预览+步骤面板+续问气泡、发布对话框+版本徽标、保存乐观锁。
**专家组页已完成**（A/B/C）：成员池（真选择器）、画布路由（Agent选择/执行节点+真 LLM 路由）、试运行、发布。

**仍缺（按调研基线）**：
| 项 | 归属切片 |
| --- | --- |
| Prompt 模板库 / AI 生成 / `#` 资源 mention 插入（调研 03 §3） | D-1 |
| 模型语义参数：生成多样性（严谨/平衡/创意）、历史轮次、工具调用辅助模型（调研 04 §3） | D-2 |
| 模型对比双卡（调研 02 §3/07 §2） | D-2 |
| 知识高级配置：深度检索/检索模式/Top K/匹配分/知识回答优先（调研 06 §3） | D-1 |
| 对话体验补全：开场白/分句回复/语音音色（调研 03 §6） | D-1 |
| 专家组流式调试抽屉（当前试运行为一次性轮询展示） | D-2 |
| 成员池 ↔ 画布 Agent选择节点联动（调研 11 §4.2：共享同一成员引用） | D-1 |
| 成员冻结版本可视化（发布时冻结的成员版本在页面可查） | D-1 |
| autonomous/expert-group 页的运行观测/效果评测/进化入口（当前仅对话编排画布有） | D-1 |

## 3. 验收基线（映射调研 §15.5 / 00 §19 / 07 §7–8）
- [x] 发布前后端双重校验；沙箱与线上独立部署记录；回滚进入审计日志（§15.5）（2026-08-25：check-e1-acceptance B2/B3/B4——409 VALIDATION_FAILED、sandbox/prod 独立 Release、回滚后 prod active 回 v1、审计含 agent.release/agent.version.create/agent.delete）
- [x] 评测任务可复现到 Agent 版本 + 数据集版本；Judge 打分样本级可查（07 §7）（79a8a08 评测做全：工作流级同步执行 + rule/model Judge + 人评覆盖 + 期望答案；eval-run/human-score 端点与面板在场）
- [x] 进化只产出候选 Patch，审批后生成新草稿；不触碰线上版本（07 §8）（/api/agents/{id}/evolution/candidates|apply|reject 端点 + AgentEvolution 面板候选/采纳/拒绝流）
- [x] 四角色权限矩阵生效；高危操作全部有审计（调研 13）（walkthrough D1：Viewer 发布禁用 / Publisher 可用 / Admin 审计页；审计动作集覆盖发布/部署/删除/规则）
- [x] 十项工程交付物齐备且与代码一致性抽查通过（00 §19）（deliverables/ 7 件：openapi+4 schema+error-codes+e2e-acceptance；+ prototypes/run-ops-prototype.html 与 2 份 design 文档；本次 E-1 抽查即一致性核验）
- [x] `grep -rn "mock-service" src` 归零（D-5 完成标志）（2026-08-25：最后一处注释措辞已改写，复核零命中）

## 4. 状态日志
- 2026-08-25 大纲建立。
- 2026-08-25 夜间细化为切片规格（用户授权连续施工的边界：D 不实现）；开工前逐切片冻结。
- 2026-08-26 D-1～D-5 全部实现完毕，待用户逐项验收：
  - D-1（a9ecf8c 前序）：自主规划四 Tab + Agent 级观测/评测面板 + 专家组画布化 + 模板库 + 知识高级配置。
  - D-2（a9ecf8c）：模型语义参数真消费（多样性→temperature/历史轮次/工具辅助模型）+ 模型对比双栏 + 语音播报。
  - D-3（a52ce1b）：评测 Judge 三类（规则/模型/人评）+ 进化 CandidatePatch（归因→候选→审批应用）。
  - D-4（f158c91）：RBAC 四角色真鉴权 + 发布门禁 + 审计日志（/settings/audit）+ 锁租约（10 分钟过期可接管）+ 密钥轮换。
  - D-5（1ea0ba8/e3551c5）：mock 双轨清退（删 mock-service/mocks-data，十页接真数据）+ 6 份工程交付物（docs/sdd/deliverables/）+ vitest（7 条）。
  - 验证：85/85 pytest + 7/7 vitest + npm build 通过 + 后端健康。
  - 验收清单见 deliverables/e2e-acceptance.md。
