# SDD 总路线图（Spec-Driven Development）

基线日期：2026-08-25
上游依据：`/Users/rivers/Documents/Codex/调研/agent-builder-research/`（00–14，v1.0，2026-08-24）
现状依据：2026-08-24/25 两轮代码审计（三型闭环完成度约 50–55%；12 处错误实现 + 8 处门面功能 + 一批缺失项）

---

## 0. 这套文档怎么用

1. **任何实现工作开始前**，必须按顺序：读本文件 → 读当前阶段规格 → 重读涉及源码（防止并行会话已改动）。
2. **规格即契约**：状态为"已冻结"的规格，实现不得偏离；确需偏离时，在该规格的"变更记录"一节追加说明（原因 + 影响），不许静默修改。
3. **验收即打勾**：每阶段完成后，对照其验收清单逐条核验，结果写入该规格"状态日志"，再更新本文件的状态看板。
4. **防忘三层**：本目录进 git；本文件的状态看板是唯一进度事实；助手持久记忆中有一条指向本文件的索引。

## 1. 文档地图

| 文档 | 阶段 | 细节级别 | 状态 |
| --- | --- | --- | --- |
| [01](01-phase-a-correctness-and-cleanup.md) | A：正确性修复与门面清退 | 可执行细节 | 草稿（待冻结） |
| [02](02-phase-b-agent-aggregate-and-release.md) | B：Agent 聚合根与发布闭环 | 可执行细节 | 草稿（待冻结） |
| [03](03-phase-c-observability-and-node-system.md) | C：运行可观测与节点系统 | 范围 + 验收基线（开工前细化） | 大纲 |
| [04](04-phase-d-product-surface-and-governance.md) | D：产品面与治理 | 范围 + 验收基线（开工前细化） | 大纲 |

## 2. 阶段状态看板

| 阶段 | 状态 | 开始 | 验收通过 | 验收人 | 备注 |
| --- | --- | --- | --- | --- | --- |
| A 正确性修复与门面清退 | 未开始 | — | — | — | 依赖：无 |
| B Agent 聚合根与发布闭环 | 未开始 | — | — | — | 依赖：A |
| C 运行可观测与节点系统 | 未开始 | — | — | — | 依赖：B；开工前先细化本规格 |
| D 产品面与治理 | 未开始 | — | — | — | 依赖：C；开工前先细化本规格 |

状态取值：未开始 / 进行中 / 待验收 / 已接受 / 已暂停。

## 3. 阶段目标速览

- **A（约 1 周）**：修掉审计发现的 12 处错误实现，处置 8 处门面功能，清退 agent 搭建轨道的遗留 mock 路径。不引入新模型、不动架构。验收主题：**"页面上能做的，运行时都真实生效"**。
- **B（约 2 周）**：Agent 成为真正的聚合根——AgentVersion/Release/revision，运行认版本（发布产物真正被执行），CommonAgentConfig 一期（对话体验 + 结构化记忆声明），autonomous 异步化 + 流式预览 + 步骤面板。验收主题：**调研 §20 四个闭环全部闭合**。
- **C（约 2.5 周）**：Trace/Span + CONTROL/CONTENT 双通道；节点注册表 `editorKinds` 与 schema 驱动 Inspector；补齐节点（对话回复/记忆变量/Query改写/决策分类/工作流选择/代码沙箱等）；变量统一为结构化绑定。验收主题：**对标调研 §15.2–15.4 的运行验收**。
- **D（持续）**：Workspace Shell 四 Tab 三型统一、模型对比、观测指标/回放、评测 Judge、进化 Patch、RBAC/审计/租约锁、工程交付物（openapi/schema/错误码）、质检页面 mock 清退。验收主题：**企业治理面**。

## 4. 工作流规则

### 4.1 开工规则
1. 读本文件与阶段规格；确认看板状态与记忆索引一致。
2. 重读规格中列出的每个待改文件的**当前内容**（并行会话可能已改动；历史教训：不重读直接 Write 会覆盖他人工作）。
3. 规格中的文件路径若已失效，先定位再在"变更记录"中登记新位置。

### 4.2 完工规则
1. 新增/修改测试全部通过（`pytest server/tests`），现有测试不回归。
2. 逐条过验收清单，产出自检报告（证据：测试输出 / 手动验证步骤）。
3. 用户逐项验收；验收结论写入规格"状态日志"与本文件看板。
4. 提交 git；更新助手记忆索引中的阶段状态。

### 4.3 变更规则
- 冻结规格的偏离必须写入该规格"变更记录"节。
- 范围蔓延（规格外的新功能）默认拒绝；确有价值则登记到对应后续阶段的"候选项"。

## 5. 全局约定（所有阶段共用）

### 5.1 错误契约（新代码一律遵守）
```json
{"code": "VALIDATION_FAILED", "message": "人类可读信息", "path": "nodes[2].config.prompt", "traceId": "..."}
```
HTTP 状态与业务状态一致（400/409/422 不再返回 200）；保留现有 409 revision 冲突语义。

### 5.2 事件命名
现有 `run_event.type` 词表保持；新增事件在所属阶段规格中登记。B 阶段起事件 payload 必须带 `runId`；C 阶段起增加 `channel/traceId/spanId`。

### 5.3 明确不做（全局）
- 不实现多人实时协同（CRDT）。
- 不实现调研中的客服工具节点（行业域）。
- 不做 QuickService 接口兼容层（调研 00 §1 明示不必复刻其接口命名）。
- 不把任何 Secret 写入 Prompt、图快照、事件正文。

### 5.4 调研 ↔ 规格对照（高频引用）
| 主题 | 调研出处 | 落地阶段 |
| --- | --- | --- |
| 四闭环总纲 | 00 §20 | A 修错、B 闭环 |
| 运行认不可变快照 | 00 §4.1、§11.3 | A-01（工作流层）、B-03（Agent 层） |
| 保存契约 revision/idempotency | 00 §10.2 | A-08、B-06 |
| 版本/发布/回滚模型 | 04 §1、09 §7、08 §10 | B |
| 变量作用域五分类 | 11 §5–6、12 §7–8 | A-04（可达性）、C（完整统一） |
| 双通道事件流 | 07 §1、08 §9、14 §4 | B（流式）、C（通道化） |
| 节点目录 editorKinds | 11 §7、12 §5–6 | C |
| Trace/Span | 07 §3 | C |
| 观测/评测/进化 | 07 §6–8 | D |

## 6. 已知技术事实快照（2026-08-25 核实）

供规格引用，避免重复勘探：

- 后端：FastAPI + SQLAlchemy + PostgreSQL（`server/`），14 个 alembic migration（B 阶段从 015 起编号）；worker 线程消费 `job_queue`（`FOR UPDATE SKIP LOCKED`），现有任务类型 `workflow-execution`。
- `notification` 节点执行器别名为 `exec_end`（runner.py EXECUTORS）；`TERMINAL_TYPES = {"end","create-record"}`。
- `Schedule.pinned_version_id` 字段存在但从未被消费。
- `create_run`/`execute_run` 一律读 `wf.draft_definition`。
- `exec_agent_select` 恒取 `primaryAgents[0]`；`cfg.routing`、`cfg.memories`、`cfg.terms`、`cfg.experiences`、`cfg.dialogue` 运行时零消费。
- `mock-service.ts` 被质检/任务/工具等多页面引用，agent 轨道清退时**不可整体删除**。
- 前端无状态管理库（纯 useState + 自写 hooks）；无前端测试框架。
- 校验器已有祖先可达性计算（`_ancestors`）与结构化绑定可达校验（R3）、prompt 引用校验（R4）。
